[project]
name = "views-baseline"
version = "0.1.0"
description = ""
authors = [
    {name = "Sonhaf-prio",email = "sonhaf@prio.org"}
]
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
]

[tool.poetry]
packages = [{include = "views_baseline"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
